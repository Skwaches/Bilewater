# Bilehaven

## Summary
This is an attempt at simulating fluids in a 2D space using the SDL library.

## Requirements 
`cmake`
`SDL` - If vendored, edit the  in the [CMakeLists.txt file](./CMakeLists.txt) to point to the location of the directory containing SDL. Otherwise, set the vendored option to OFF.
